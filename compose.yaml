services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=finaltask'
      - 'MYSQL_PASSWORD=1234'
      - 'MYSQL_ROOT_PASSWORD=root'
      - 'MYSQL_USER=springuser'
    ports:
      - '3306'
